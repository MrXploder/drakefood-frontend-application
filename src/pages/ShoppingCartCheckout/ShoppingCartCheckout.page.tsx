import ShoppingCartPreview from '@/components/ShoppingCartPreview/ShoppingCartPreview.component'

export default () => {
  return (
    <section className='w-[50%] flex flex-row mx-auto'>
      <ShoppingCartPreview />
    </section>
  )
}
